<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BowlProMobile"
        tools:targetApi="31">

        <activity android:name=".TelaLoginCliente"
            android:exported="false"/>

        <activity android:name=".TelaCadastroCliente"
            android:exported="false"/>

        <activity android:name=".Menu_Cliente"
            android:exported="false"/>

        <activity android:name=".TelaAgendarHorario"
            android:exported="false"/>

        <activity android:name=".TelaExibirHorario"
            android:exported="false"/>

        <activity android:name=".TelaPerfil"
        android:exported="false"/>

        <activity android:name=".TelaMenu_ADM"
            android:exported="false"/>

        <activity android:name=".TelaCod_Only"
            android:exported="false"/>

        <activity android:name=".TelaCadastrarFun"
            android:exported="false"/>

        <activity android:name=".BuscarFun"
            android:exported="false"/>



        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>